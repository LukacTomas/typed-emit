/**
 * Lightweight, generic typed event emitter suitable for publishing as a small
 * library. This file intentionally does not reference application-specific
 * event types so it can be reused.
 */
export * from './types';
export * from './typed-event-emitter';
